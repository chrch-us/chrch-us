<%= form_for :new_join, url: new_joins_path, html: {class: 'join'} do |f| %>
  <%= f.email_field :email, name: "email", placeholder: "enter an email address", required: true, class: "form-control email" %>
  <%= f.submit 'join' %>
<% end %>